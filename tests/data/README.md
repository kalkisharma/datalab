# tests/data — Test Dataset Specifications

Owned by the QA Engineer. Update this file whenever a new dataset is needed for testing.

## Committed synthetic datasets

Synthetic datasets are generated by QA and committed to this directory.
- Max 500KB per file
- Naming convention: `test_{descriptor}_{rows}rows.csv`

None committed yet — first datasets added in Phase 1 (smoke render) and Phase 2 (benchmark).

## Required datasets by phase

### Phase 1 — smoke render
- `test_scatter_100rows.csv` — 2 numeric columns (x, y), 100 rows
  - Purpose: smoke render test (`tests/smoke.spec.js`)
  - Generate: uniform random values, x ∈ [0, 100], y ∈ [0, 100]

### Phase 2 — benchmark
- `test_benchmark_50000rows.csv` — 4 columns (x, y, category, value), 50,000 rows
  - Purpose: warm render benchmark (`tests/bench.spec.js`)
  - Spec: x/y numeric, category has 10 unique values, value numeric
  - Performance Engineer must sign off on this spec before the file is generated

### Phase 3 — renderer validation
- `test_datetime_iso_500rows.csv` — date column (ISO 8601), numeric y, 500 rows
- `test_datetime_mmddyyyy_500rows.csv` — date column (MM/DD/YYYY), numeric y, 500 rows

## Real-world datasets (not committed)

Real-world CSVs used for Data Scientist exploratory testing must NOT be committed
(may contain sensitive information). Document what is needed here for reference:

- Phase 1 exploratory test: any two real CSVs with numeric columns suitable for
  scatter and parity plots. Maintainer: Kalki Sharma (kalkijsharma@gmail.com).
